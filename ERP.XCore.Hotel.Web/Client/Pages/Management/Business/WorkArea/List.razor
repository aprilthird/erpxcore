@page "/maestros/empresas/cargo"

@using ERP.XCore.Entities.Models
@using ERP.XCore.Hotel.Shared.Helpers
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication
@inject HttpClient Http

<div class="row px-5">
	<div class="d-flex justify-content-between">
		<h3>Area</h3>
		<button class="btn btn-primary" @onclick="() => ShowModalAsync(ModalType.New)">
			<i class="fa fa-add"></i>
			<span>Agregar</span>
		</button>
	</div>
	<div class="col-md-12">
		<div class="table-responsive">
        @if(ItemsList == null)
		{
			<p><em>Cargando...</em></p>
		}
		else
		{
			<table class="table table-bordered table-striped">
				<thead>
					<tr class="fw-bold">
						<th>Descripción</th>
						<th>Acciones</th>
					</tr>
				</thead>
				<tbody>
					@foreach(var item in ItemsList)
					{
						<tr>
							<td>@item.Description</td>
							<td>
								<button class="btn btn-info" @onclick="() => ShowModalAsync(ModalType.Edit, item)">
									<i class="fa fa-edit"></i>
									<span>Editar</span>
								</button>
								<button class="btn btn-danger" @onclick="() => DeleteAsync(item)">
									<i class="fa fa-trash"></i>
									<span>Eliminar</span>
								</button>
							</td>
						</tr>
					}
				</tbody>
			</table>
		}
		</div>
	</div>
</div>

<ModalForm @ref="Modal" Title="@ModalTitle" Model="WorkArea" ValidSubmitCallback="SaveAsync" InvalidSubmitCallback="ShowError">
    <Fields @ref="Fields" WorkArea="WorkArea" />
</ModalForm>

@code {
	private WorkArea WorkArea = new WorkArea();
	private ModalForm Modal;
	private Fields Fields;
	private string ModalTitle;
	private WorkArea[]? ItemsList;

	public enum ModalType
	{
		New, Edit
	};

	protected override async Task OnInitializedAsync()
	{
		try
		{
			await Load();
		}
		catch (AccessTokenNotAvailableException exception)
		{
			exception.Redirect();
		}
	}

	public async Task Load()
	{
		ItemsList = await Http.GetFromJsonAsync<WorkArea[]>(RouteConfig.Management.Business.WORK_AREA_ROUTE);
	}

	public async Task ShowModalAsync(ModalType modalType, WorkArea workArea = null)
	{
		ModalTitle = $"{(modalType == ModalType.New ? "Nuevo" : "Editar")} Cargo";
		WorkArea = workArea ?? new WorkArea();
		await Modal.OpenAsync();
	}

	public async Task SaveAsync()
	{
		try
		{
			if(WorkArea.Id == Guid.Empty)
			{
				await Http.PostAsJsonAsync(RouteConfig.Management.Business.WORK_AREA_ROUTE, WorkArea);
			}
			else
			{
				await Http.PutAsJsonAsync(RouteConfig.Management.Business.WORK_AREA_ROUTE + "/" + WorkArea.Id, WorkArea);
			}
			await Load();
			await Modal.CloseAsync();
		}
		catch(Exception ex)
		{

		}
	}

	public void ShowError()
	{
	}

	public async Task DeleteAsync(WorkArea workArea)
	{
		await Http.DeleteAsync(RouteConfig.Management.Business.WORK_AREA_ROUTE + "/" + workArea.Id);
		await Load();
    }
}
